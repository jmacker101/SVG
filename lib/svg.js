const { Circle } = require("./shapes");

//declare class SVG to render SVG logo using <svg>
class SVG
// do i take the render code from the shapes.js as the same render code
//display svg line of element described by function parameter
const svgParent = document.getElementById(“svg-parent”);
const svgElement = document.getElementById("line-one");
svgParent.className.baseVal = “”; //remove class "invisible"
svgElement.className.baseVal = “”; //remove class "invisible"

// class SVG
// is line 13 and 14 correct when it comes to putting the svg together?
// <svg width='150' heigh= '150'>
//     <Circle cx="50" cy="50" r="40" stroke="green" stroke-width="4" fill="yellow" />
// </svg>
//within that class declare 2 methods called setText and setCShape
// so confuse about the two clase declare methods? and having trouble finding them when i look them up.
XMLDocument.setText()


//export the class SVG
